# Geographically-Weighted-Regression-for-Causal-Inference
This code performs geographically weighted regression on a semi-synthetic spatial dataset. It then compares results to other causal inference models to see if GWR is a valid CI model for a given dataset. 
